@model Acadon.ViewModels.CasViewModel

@{
    ViewData["Title"] = "Create Cas";
}

<h1>Create Cas</h1>

<h4>Cas</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="PredmetNaziv" class="control-label"></label>
                <input asp-for="PredmetNaziv" class="form-control" />
                <span asp-validation-for="PredmetNaziv" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="RazredNaziv" class="control-label"></label>
                <input asp-for="RazredNaziv" class="form-control" />
                <span asp-validation-for="RazredNaziv" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Vrijeme" class="control-label"></label>
                <input asp-for="Vrijeme" class="form-control" type="datetime-local" />
                <span asp-validation-for="Vrijeme" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
